# Base de datos
DATABASE_URL=postgresql://${PGUSER}:${PGPASSWORD}@${PGHOST}:${PGPORT}/${PGDATABASE}

# Autenticación
JWT_SECRET=cambia_esto_por_una_clave_segura_para_produccion
JWT_REFRESH_SECRET=cambia_esto_por_otra_clave_segura_para_produccion

# Configuración del servidor
PORT=3000
NODE_ENV=production

# Correo electrónico
SMTP_HOST=smtp.ejemplo.com
SMTP_PORT=587
SMTP_USER=tu_usuario
SMTP_PASS=tu_contraseña
DEFAULT_FROM_EMAIL=noreply@tudominio.com

# Frontend URL (no necesario en modo todo-en-uno)
FRONTEND_URL=
